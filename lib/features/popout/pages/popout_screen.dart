import 'package:flutter/material.dart';

class PopOutScreen extends StatelessWidget {
  const PopOutScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Pop out')));
  }
}
